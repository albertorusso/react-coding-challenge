import React from 'react';
import TableContent from 'components/content/TableContent'

const Body = (props) => {
    return (
        <React.Fragment>
            <TableContent />
        </React.Fragment>
    )
}

export default Body